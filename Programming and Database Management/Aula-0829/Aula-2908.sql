/* Criar um bloco PL/SQL que valide um número de CPF informado pelo usuário.

O bloco deve retornar, após informado o CPF se ele é válido ou inválido.

Crie pelo menos uma exceção no BLOCO. Garanta que somente CPFs com 11 dígitos sejam validados */

SET SERVEROUT ON

//REGRAS:

DECLARE

BEGIN

END;
/